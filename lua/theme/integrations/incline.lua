local colors = require("theme").get_theme_tb "base_30"

return {
  InclineNormal = {
    fg = colors.black,
    bg = colors.green,
  },
  InclineNormalNC = {
    fg = colors.light_grey,
    bg = colors.lightbg,
  },
}
